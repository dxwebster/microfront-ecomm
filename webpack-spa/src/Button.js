import React from 'react';

const style = {
  background: '#800',
  color: '#fff',
  padding: 12,
};

const Button = () => <button style={style}>Botão do projeto webpack JS</button>;

export default Button;
